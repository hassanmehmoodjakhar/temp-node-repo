// npm - global command comes with node
// npm --version            //Both npm and node may have different versions

// local dependencies - use it only in this particular project
// npm i <packageName>          // for only required package

// global dependency - use it in any project
// npm install -g <packageName>
// sudo npm install -g <packageName> (mac)

// We created a temp repo and a gitingnore files to ignore the node modules by source control
